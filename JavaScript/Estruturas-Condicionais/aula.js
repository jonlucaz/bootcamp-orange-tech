const numero = 4;
const isPar = numero % 2 === 0;
if (isPar) {
  console.log("O número indicado é par");
} else {
  console.log("O número indicado é impar");
}

//quando a váriavel é booleana é convenção no JS colocar is na frente do nome
